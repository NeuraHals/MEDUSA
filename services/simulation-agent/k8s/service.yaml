apiVersion: v1
kind: Service
metadata:
  name: simulation-agent
  namespace: medusa-simulation
spec:
  selector:
    app: simulation-agent
  ports:
  - name: http
    port: 8090
    targetPort: 8090
  type: ClusterIP
