apiVersion: apps/v1
kind: Deployment
metadata:
  name: oversight-agent
  namespace: medusa-orchestration
  labels:
    app: oversight-agent
    version: v1.0.0
spec:
  replicas: 3
  selector:
    matchLabels:
      app: oversight-agent
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: oversight-agent
    spec:
      priorityClassName: critical-execution
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
      containers:
      - name: oversight-agent
        image: antigravity/oversight-agent:v1.0.0
        imagePullPolicy: Always
        ports:
        - containerPort: 8085
          name: http
        - containerPort: 50051
          name: grpc
        envFrom:
        - configMapRef:
            name: oversight-agent-config
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "1Gi"
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8085
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8085
          initialDelaySeconds: 15
          periodSeconds: 20
          failureThreshold: 3
