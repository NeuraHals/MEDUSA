apiVersion: v1
kind: Service
metadata:
  name: confidence-agent
  namespace: medusa-orchestration
  labels:
    app: confidence-agent
spec:
  selector:
    app: confidence-agent
  ports:
  - name: health
    protocol: TCP
    port: 8082
    targetPort: 8082
  type: ClusterIP
